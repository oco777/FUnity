/* actors.uss — shared actor styles */
.actor {
    flex-shrink: 0;
}

.portrait,
#portrait {
    width: 100%;
    height: 100%;
    background-size: contain;
    background-position: center;
    background-repeat: no-repeat;
}

.actor-root {
    position: absolute;
}
