Import this sample through the Unity Package Manager to see a minimal FUnity setup.
After opening **Samples~/BasicScene/FUnitySample.unity**, run the menu command **FUnity/Create/FUnityProjectData** so that the scene loads the default project, stage, and actor assets.
Add the SampleController component to a GameObject and press Play to view the console output.
